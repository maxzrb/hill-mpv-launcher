# Hills 外部 mpv Launcher 使用说明

## 1. 准备

1. 解压发布 ZIP 到一个固定目录。
2. 确认电脑已经安装可正常运行的 Windows x64 版 mpv。
3. 直接双击 `mpv-launcher.exe`，按配置向导填写本机信息；也可以在命令行执行 `mpv-launcher.exe --setup`。

## 2. 配置向导

向导会依次询问 mpv 可执行文件路径、mpv 工作目录、Emby 服务地址和可选 token。token 输入不会回显；回车会保留已有 token，输入 `CLEAR` 可以清除已有 token。

向导只修改 exe 同目录的用户侧 `launcher.ini`，不会修改仓库文件，也不会把 token 写入日志。Hills 带播放参数启动时，launcher 会直接播放，不会进入向导。

## 3. 手动修改 launcher.ini

```ini
[mpv]
path=C:\你的mpv目录\mpv.exe
working_directory=C:\你的mpv目录

[emby]
server=https://你的Emby服务器地址
token=
```

- `path` 必须指向真正的 `mpv.exe`。
- `working_directory` 填 mpv 便携配置所在目录；如果使用普通安装，可填 mpv 所在目录。
- `server` 填 Hills 使用的 Emby 服务地址。
- `token` 通常留空，wrapper 会优先读取 Hills 当前服务器的本地会话。只有自动读取不可用时才在本机填写，不能把 token 发给别人或提交到 Git。
- `extra_args` 默认留空，不要为了“解决加载慢”自行加入强制全屏或覆盖起播位置的参数。

## 4. 在 Hills 中设置

在 Hills 设置中，将“外部播放器 / mpv 路径”设置为发布目录里的 `mpv-launcher.exe`，不要直接选择真正的 `mpv.exe`。

## 5. 播放与进度

wrapper 会保留 Hills 传入的播放参数，对远程媒体增加必要的稳定性保护，并把 mpv 的 stdout reporter 回传给 Hills。手动关闭、停止或播放错误不会被伪报为正常播放完毕。

## 6. 故障排查

日志位于 exe 同目录的 `logs` 文件夹，文件名为 `launcher-YYYYMMDD.log`。提供日志给他人前，请先确认其中没有本机路径、服务器地址或其他隐私信息；不要提供 `launcher.ini` 中的 token。

可以使用以下诊断开关：

```text
mpv-launcher.exe --help
mpv-launcher.exe --dry-run <Hills传入的参数>
mpv-launcher.exe --debug <Hills传入的参数>
```

`--dry-run` 只生成并记录最终命令，不启动 mpv；敏感字段会按规则脱敏。
