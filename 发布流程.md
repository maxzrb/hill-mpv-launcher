# hill-mpv-launcher 发布流程（强制）

> 本文件是 `hill-mpv-launcher` 唯一的发布规范。每次发布、打包、创建 tag 或上传 GitHub Release 都必须按本文执行。
>
> **不可擅自修改：** 未经用户在当前对话明确授权，任何 agent 不得修改、重命名、删除、拆分、绕过或降低本文要求。若任务需要改变本文，必须先停止发布并询问用户；在获得明确授权前，不得继续发布。

## 1. 发布目标

- GitHub 仓库：`maxzrb/hill-mpv-launcher`
- 仓库可见性：公开
- 目标平台：Windows x64
- Release 上传内容：一个 ZIP，包含脱敏后的 `mpv-launcher.exe`、`launcher.ini` 和 `使用说明.md`
- Release 不单独上传 exe、ini、日志、源码压缩包或本机配置

## 2. 版本号与 tag

- 版本号必须由用户明确确认，格式严格为 `X.Y.Z`，例如 `1.0.0`。
- 首个版本已确认使用 `1.0.0`。
- Git tag 严格写为 `vX.Y.Z`，首个 tag 严格写为 `v1.0.0`。
- tag 只能包含版本号及其固定的 `v` 前缀，不得写成 `launcher v1.0.0`、`hill-mpv-launcher-v1.0.0` 或任何其他混合格式。
- agent 不得自行猜测、递增、重写或复用版本号；发现 tag 已存在时必须停止并报告。

## 3. Release Notes 规则

- 每个要点必须单独占一行。
- 每行开头只能使用以下三种标签：`[修改]`、`[新增]`、`[移除]`。
- 不得自行新增 `[流程]`、`[修复]`、`[安全]`、`[变更]` 等其他标签。
- 如果 agent 认为确有必要增加标签，必须先询问用户，得到明确同意后才可以修改本规则和使用新标签。
- Release 标题使用 `vX.Y.Z`，与 tag 保持一致，不混入项目名。
- 发行说明模板：

```text
[修改] 每个修改事项一行
[新增] 每个新增事项一行
[移除] 每个移除事项一行
```

## 4. 发布前记录与检查

1. 完整阅读本文件、项目 `AGENTS.md`、`docs/codex/STATUS.md` 和现有版本记录。
2. 检查 Git 工作区、当前分支、远端和待发布 tag；不得覆盖用户未提交的改动。
3. 检查公开文件和待打包目录，不得出现以下内容：
   - Token、API key、Cookie、Authorization、密码或 CDN 签名；
   - `C:\Users\...` 等真实本机用户路径、计算机名或个人环境信息；
   - 真实 Hills/Emby 登录数据、调试日志和测试输出；
   - 未经用户授权发布的第三方专有文件。
4. 将脱敏检查结果写入 `docs/codex/STATUS.md`，检查失败则停止发布。

## 5. 构建与验证

在项目目录执行：

```powershell
dotnet build .\HillsMpvLauncher.csproj --configuration Release --nologo
dotnet publish .\HillsMpvLauncher.csproj --configuration Release --runtime win-x64 --self-contained true --nologo --output .\bin\Release\net8.0-windows\win-x64\publish
```

- build 和 publish 必须均为 0 警告、0 错误。
- 必须验证发布 exe 存在、为 Windows x64 self-contained 单文件，并通过 `--help` 或等效无媒体启动检查。
- 必须保留已有的功能回归结论；本项目至少检查远程媒体参数解析、S1E7/S1E8/S1E9 多集顺序、stdout reporter 和敏感信息脱敏。

## 6. 组包规则

1. 使用全新的临时 staging 目录，不复制 `logs`、`bin`、`obj`、`test-output` 或本机 `launcher.ini`。
2. staging 只允许包含：
   - `mpv-launcher.exe`；
   - 脱敏后的 `launcher.ini`，其中 mpv 路径和服务器地址必须是通用占位值；
   - `使用说明.md`。
3. ZIP 文件名严格为 `hill-mpv-launcher-vX.Y.Z-win-x64.zip`；该文件只作为 Release asset，不提交到源码仓库。
4. 检查 ZIP 清单、压缩包完整性和 SHA-256；压缩包中不得出现额外文件或敏感内容。

## 7. Git 与 GitHub Release

1. 首次发布时初始化项目 Git 仓库，默认分支为 `main`，只提交经过脱敏审计的源码、文档和流程文件。
2. 创建并推送 tag：`vX.Y.Z`。
3. 使用 GitHub CLI 创建或确认 `maxzrb/hill-mpv-launcher`，再创建正式 Release：

```powershell
gh release create vX.Y.Z .\release\hill-mpv-launcher-vX.Y.Z-win-x64.zip --title vX.Y.Z --notes-file .\release\release-notes-vX.Y.Z.txt
```

4. Release 必须为正式发布，不得默认创建 draft 或 prerelease。
5. 上传后检查远端 tag、Release 标题、Release Notes、asset 文件名、大小和 SHA-256；远端结果不一致则停止并报告，不覆盖重发。

## 8. 发布后记录

- 更新 `docs/codex/STATUS.md` 顶部当前快照，并在文件末尾追加带时间的完整发布日志。
- 在 `version/工作进度.md` 末尾追加用户可读的发布结果。
- 更新 `version/版本迭代记录.md`，记录 `vX.Y.Z`、日期、Release URL、ZIP SHA-256、兼容性和迁移说明。
- 将最终 Git 分支、commit、tag、远端 Release URL 和工作区状态写入状态记录。
- 发布完成后仍不得删除或改写历史记录。

## 9. 失败处理

- 任一强制检查失败、发现敏感信息、版本/tag 不明确、tag 已存在、流程发生冲突或远端状态不一致时，立即停止发布并向用户报告原因。
- 不通过删除日志、重写历史、强制 push、覆盖 tag 或修改本文件来掩盖失败。
