# Hills 外部 mpv Launcher 工作进度

## 2026-09-06 13:36

### 第一阶段完成

- 在独立项目目录建立 Windows C#/.NET 8 launcher，发布为 self-contained 单文件 `mpv-launcher.exe`。
- 完成 Hills 参数完整采集：原始命令行、解析后的 argv、工作目录、启动时间、父进程和环境变量均会记录。
- 完成安全日志：日志为 UTF-8，默认遮盖 API key、Token、Cookie、签名、Authorization 和 115 CDN 查询参数。
- 完成 URL 检查：识别 Emby 会话地址、115 CDN 地址和辅助字幕/音频/Referer 地址，记录 URL 长度、短哈希、主机、特殊字符、重复协议和 URI 合法性。
- 同时存在 Emby URL 与 CDN URL 时优先转发 Emby URL；只有 CDN URL 时不猜测 ItemId，原样回退并明确记录。
- 使用 `CreateProcessW` 安全启动真实 mpv，保留 Hills 参数、标题、字幕、音轨和起始时间，并等待子进程结束后返回退出码。
- `dotnet build`、self-contained `dotnet publish`、特殊字符/中文/敏感字段测试、Emby URL 优先测试、CDN 原样回退测试和 CreateProcessW 模拟测试均通过。
- 尚未实现 Emby PlaybackInfo 重取链；下一步必须先用真实 Hills 调用日志确认是否有可靠的 Server、ItemId、MediaSourceId 和 Token 上下文。

## 2026-09-06 13:38

### 最终启动安全校验

- 在真正调用 `CreateProcessW` 前，新增 `CommandLineToArgvW` 往返校验，逐项确认 mpv 路径和所有参数与原始计划完全一致。
- 重新完成 Release 编译和 self-contained 单文件发布；包含特殊字符、中文、空格、签名参数、Header 的测试均通过，敏感值未进入日志。

## 2026-09-06 13:40

### 发布目录配置示例

- `launcher.ini.example` 已配置为自动复制到 Release 与 self-contained publish 目录；最终发布目录可直接复制示例并填写真实 mpv 路径。
- 临时测试日志已清理，真实 Hills 运行时会在 exe 同目录自动创建 UTF-8 日志。

## 2026-09-06 13:41

### 脱敏收尾

- fatal debug 异常和错误配置行也纳入统一脱敏；最终 self-contained 发布再次通过。

## 2026-09-06 13:53

### 已完成可直接使用的配置

- 发布目录已生成脱敏模板 `launcher.ini`；使用者需要按本机 mpv 路径和服务器地址编辑配置。
- 已用包含 Emby URL、特殊字符和 API key 的模拟参数完成 `--dry-run` 验证；命令行往返校验通过，日志未泄露敏感值。
- Hills 端只需将“外部播放器 / mpv 路径”改为发布目录中的 `mpv-launcher.exe`。

## 2026-09-06 14:17

### 真实失败原因确认与取链修复

- 真实 Hills 调用只传递 115 CDN 直链，没有 Emby URL、ItemId、MediaSourceId 或 Token；所以此前 wrapper 仍会把失效签名原样交给 mpv。
- 新增 Hills 本地缓存解析：仅在 `--force-media-title` 与 `rememberTracks.sourceName` 可靠匹配时，读取 ItemId/MediaSourceId，改用 Emby 会话地址，让 Emby 重新生成 115 签名；无法匹配时不猜 ID。
- 已支持在本地配置 Emby 地址和 mpv 工作目录；公开文档不保留本机服务器或路径。
- 生成的会话地址实测跟随 302 后返回 HTTP 206；Release build、publish、命令行往返校验均通过。
- 修正 `*_API_KEY` 环境变量日志脱敏，并处理旧诊断日志的敏感值。

## 2026-09-06 14:22

### 取消默认全屏

- 移除 launcher 配置中的 `--fs`，外部 mpv 现在按原有窗口配置启动，不再默认占满屏幕。
- 已通过 `--dry-run` 确认最终命令行不再追加 `--fs`，Emby 重新取链逻辑不受影响。

## 2026-09-06 15:06

### 第二阶段稳定性增强完成

- 响应缓存解析兼容单媒体详情和 `Items` 列表；新增文件名/媒体源名/标题评分、同分拒绝和多点号文件名处理，支持 `rememberTracks` 未命中的媒体。
- mpv stdout 改为管道 tee：一份原样回传父进程供 Hills reporter 使用，一份写入脱敏日志；stderr 同步采集，网络/403/签名/解码/打开失败分类可审计。
- 已重新执行 self-contained publish；发布目录 `launcher.ini` 已恢复为真实 mpv 路径，`extra_args` 保持为空，不强制全屏。
- dry-run、响应缓存 fallback、stdout 回传、模拟 `HILLS_MPV_EVENT:` 和本机拒绝连接错误分类均通过。

## 2026-09-06 15:17

### 修正手动关闭误报播放完成

- 根据真实 Hills 日志确认：`time-pos` 事件持续正常转发，但 mpv 关闭时 reporter 发出 `end-file(reason=quit,time_pos=0)`；Hills 将其误判为播放完成。
- wrapper 现在只透传 `end-file(reason=eof)`；手动关闭、停止和错误会抑制完成事件，并将最近一条 `time-pos` 再回传一次。
- 已重新 build/publish；模拟 `quit` 与 `eof` 两条路径均通过，发布配置已恢复真实 mpv 且不强制全屏。

## 2026-09-06 15:54

### 修正多集首集失败后自动跳集

- 从真实 Hills 日志确认：一次调用包含第 7、8、9 集三个 `--{ ... --}` 媒体块，旧逻辑只替换第一个 URL；第一个还因误用季 ID返回 401，后两集继续使用过期 115 URL并返回403，mpv随后自动尝试下一集。
- wrapper 现在按媒体块分别读取自己的 `--force-media-title`，逐个匹配和替换 115 URL；缓存缺少后续集时，使用 Hills 当前服务器会话查询 Emby 媒体列表。
- Hills `rememberTracks` 的剧集记录保存的是季 ID，`MediaSourceId` 的数字部分对应具体视频 ID；生成会话地址时已改用视频 ID。
- 新增只读读取 Hills `hills_database.sqlite` 的当前服务器 AccessToken，令牌仅在内存中用于 Emby API/媒体会话，不写配置、不进日志；显式 `[emby] token` 仍优先。
- 已通过 Release build、self-contained publish 和三媒体块 dry-run：三集均生成对应 Emby 视频地址，日志显示敏感令牌为 `<redacted>`。

## 2026-09-06 16:06

### 多集取链回归修正与最终发布

- 发现 API 短缓存若保存上一集的匹配分数，可能让第 9 集复用第 8 集；已改为缓存原始候选并按当前媒体块重新评分。
- 最终三媒体块 dry-run 通过：第 7、8、9 集分别生成对应的 Emby 会话地址。
- 最终 Release build 和 self-contained publish 均为 0 警告、0 错误；发布目录配置保持真实 mpv 路径、无强制全屏参数。
- 对发布日志执行敏感字段审计通过，未发现未脱敏令牌、Authorization、Cookie 或 115 签名；无残留 mpv 测试进程。

## 2026-09-06 16:48

### 修复 S1E7 起播误判损坏后自动跳集

- 真实日志确认 S1E7 并非取链或鉴权失败，而是已加载到 43 秒后，被 startup-format-logos 的远程 ffmpeg 后瞻探测与主流并发触发 `mkv` 损坏误判，随后 Hills 收到错误结束并跳到下一集。
- wrapper 现在对 Emby/115 等远程媒体自动追加 `--script-opts-append=startup_format_logos-mode=none`，只关闭远程文件的黑边后瞻探测；本地文件不受影响，用户显式指定模式时不覆盖。
- 已完成 Release build、self-contained publish、dry-run 及真实单集回归：S1E7 成功加载并持续播放，未再出现 `Corrupt file detected`、Lua `tonumber` 错误或自动跳集。
- 发布文件仍位于项目的 `bin/Release/net8.0-windows/win-x64/publish/mpv-launcher.exe`，Hills 设置无需再改参数。

## 2026-09-06 17:07

### 修复 S1E7 卡加载及旧 playlist 恢复

- 最新 Hills 日志发现 mpv 输出 `Resuming playback`，先恢复到旧的 playlist 位置 1（S1E8），导致 S1E7 迟迟不能按当前集进入播放。
- wrapper 对远程媒体自动追加 `--no-resume-playback`；Hills 自己传入的 `--start` 保留，显式设置 resume 参数时不覆盖。
- 已重新完成 Release build、self-contained publish、dry-run 和真实单集回归；S1E7 按指定起始位置加载并持续运行。

## 2026-09-06 17:11

### 三集 playlist 起播顺序验证

- 使用 S1E7、S1E8、S1E9 三集参数完成真实解析和启动回归。
- 日志确认首个播放项直接为 S1E7，不再被 mpv watch-later 恢复到 S1E8；测试进程已清理。

## 2026-09-06 17:31

### 建立受保护发布流程并准备首个版本

- 已建立项目级《发布流程.md》，并设置为 Windows 只读；未经用户明确授权，agent 不得修改、绕过或降低流程要求。
- 首个版本确定为 `1.0.0`，release tag 严格使用 `v1.0.0`，不会混入项目名。
- Release Notes 已固定为每个要点单独一行，只使用 `[修改]`、`[新增]`、`[移除]`；如需新增标签，必须先询问用户。
- 已完成源码、说明文档和配置示例的脱敏，下一步执行最终构建、ZIP 清单校验和远端发布。
